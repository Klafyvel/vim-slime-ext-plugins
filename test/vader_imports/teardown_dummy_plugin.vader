After (Cleanup dummy plugin):
  unlet g:test_last_config_received_by_TestTarget
  unlet g:test_last_config_received_by_TestConfig
  unlet g:test_last_text_received_by_TestTarget
  delfunction TestTarget
  delfunction TestConfig

  if exists("g:slime_target_config_old")
    let g:slime_target_config = g:slime_target_config_old
    unlet g:slime_target_config_old
  else
    unlet g:slime_target_config
  endif
  if exists("g:slime_target_send_old")
    let g:slime_target_send = g:slime_target_send_old
    unlet g:slime_target_send_old
  else
    unlet g:slime_target_send
  endif
